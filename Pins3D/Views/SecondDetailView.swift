import SwiftUI

struct SecondDetailView: View {
    var body: some View {
        VStack {
            Text("Second Detail View")
            // Add any NavigationLink to another view if needed.
        }
    }
}
